# 历史消息解析方案

本方案已实施（v0.2.0），解析与配对逻辑在 `frontend/src/chat-stream.js`，规则以代码为准。

## 1. 收到的原始数据

```text
Hermes dashboard 历史接口
  → 自研后端原样转发
  → 前端收到 messages + pagination
  → 解析正文、内容提示、工具摘要
  → 展示消息
```

保留消息 ID、顺序及分页信息；前端不直连 Hermes。

## 2. 解析规则

| 原始数据 | 解析 | 展示 |
|---|---|---|
| 字符串正文 | 原样保留，不对普通 JSON 文本二次解析 | 文本气泡 |
| 数组、对象正文 | 递归提取已确认的 `text`、`output_text`、`content`、`message` 文本字段 | 文本气泡 |
| 图片、音频、文件块 | 保留相邻文字，识别非文本类型 | 文字 + 内容类型提示，不加载媒体或外链 |
| 未知结构 | 不直接输出整个对象，不静默丢弃 | “暂不支持的内容”提示 |
| `display_content` | 存在时优先使用；显式空值不回退到原始正文或附加字段 | 展示投影内容，不暴露内部摘要 |
| `hidden` / `display_kind=hidden` | 优先过滤，不恢复隐藏内容 | 不展示 |
| `codex_message_items` | 仅在无展示投影、正文为空且非隐藏时，解析数组或 JSON 字符串；提取 assistant message 中的 text/output_text，排除 analysis/commentary | 恢复正式答案 |
| `tool_calls` / `role=tool` | 合并消息页后，按调用 ID 在同一用户回合内关联；缺少 ID 不猜配对 | 工具名称 + “已有结果”或“未找到结果”，不推断成功或运行中 |
| 仅含思考记录 | 与正式答案分开 | “仅含思考记录”提示 |
| 真正空消息 | 保留定位信息，不生成空白气泡 | 不展示 |

## 3. 展示与对账

- 正文、内容提示、工具摘要分开；本地消息对账只比较规范正文，提示不能当作答案。
- 分页仍按 `pagination.returned` 推进，不按可见气泡数计算。
- 刷新、加载更早消息后按消息 ID 去重，避免历史工具摘要与实时工具记录重复。
- 本次不增加 Markdown 渲染、媒体预览、工具参数详情或完整思考展示。
